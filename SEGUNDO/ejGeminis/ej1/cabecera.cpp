#include "cabecera.h"

int inversion(int* v, int x0, int x1) {
    if(x1 - x0 == o) return 1;
    int mitad = (x0+x1)/2;
    
    int mitadI = inversion(v, x0, mitad);
    if(v[x0>v[x1]]) aux++;
    int mitadD = inversion(v, mitad, x1);

    


}

int inversion(int* v, int n) {
    return inversion(v, 0, n);
}